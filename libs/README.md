# Libraries

Use this directory for cross-project libraries generated by Nx, for example:

- `libs/logger`
- `libs/types`
- `libs/utils`

Create a new shared library:

```bash
npx nx g @nx/js:library libs/<lib-name>
```
