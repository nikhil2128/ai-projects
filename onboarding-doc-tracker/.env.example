# ── Server (local development only) ──
PORT=3005
NODE_ENV=development

# ── Microsoft Azure AD App Registration ──
# Register at: https://portal.azure.com → Azure Active Directory → App registrations
# Required API permission (Application type, not Delegated):
#   - Files.ReadWrite.All (create folders and upload to OneDrive)
AZURE_TENANT_ID=your-tenant-id
AZURE_CLIENT_ID=your-client-id
AZURE_CLIENT_SECRET=your-client-secret

# ── HR Configuration ──
# HR email address to receive document upload notifications
HR_EMAIL=hr@company.com
# Microsoft Graph user ID for the HR mailbox (OneDrive owner)
# Find via: GET https://graph.microsoft.com/v1.0/users/hr@company.com → id
HR_USER_ID=hr-user-object-id
# OneDrive root folder for onboarding documents
ONEDRIVE_ROOT_FOLDER=Onboarding Documents

# ── AWS ──
AWS_REGION=us-east-1
DYNAMODB_TABLE=onboarding-doc-tracker
# S3 bucket where SES stores incoming emails
EMAIL_BUCKET=onboarding-doc-emails

# ── SES ──
# Verified sender address for HR notification emails
SES_FROM_EMAIL=onboarding@company.com
